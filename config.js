const config = {
  // 启动端口
  port: 3001,
  // 数据库配置
  database: {
    DATABASE: 'koa_node',
    USERNAME: 'root',
    PASSWORD: 'lin84aijun1314',
    PORT: '3306',
    HOST: 'localhost'
  }
};

module.exports = config;